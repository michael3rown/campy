<nav class="navbar navbar-light">
  <ul class="navbar-nav">
    <li class="nav-item">{{#bs-button type="primary" size="sm" onClick=(action "navToggle")}}|{{/bs-button}}</li>
    {{#each this.data as |campground|}}
      <li class="nav-item">{{#link-to "detail" campground.id class="nav-link"}}{{campground.name}}{{/link-to}}</li>
    {{else}}
      <li class="nav-item"><span class="nav-link font-italic">Sorry, there are no campgrounds yet.</span></li>
    {{/each}}
  </ul>
</nav>